using UnityEngine;
using System.Collections;

public class BaseScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = SharedVariables.baseLocation; 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
